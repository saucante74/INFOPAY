# Dev-only tooling. Kept out of requirements.txt on purpose: the Dockerfile
# installs requirements.txt into the runtime image, and type-checking tools
# have no business shipping to production.
#
#   pip install -r requirements-dev.txt
#   mypy --strict app/

-r requirements.txt

mypy==1.13.0
pandas-stubs==2.2.3.241009
